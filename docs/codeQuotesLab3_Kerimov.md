# Блоки кода и цитаты

## Код

### Встроенный (inline) код
Команда `git status` показывает текущее состояние рабочего каталога и индекса.

### Блок кода с подсветкой синтаксиса (Python)
```python
def greet(name):
    print(f"Hello, {name}!")

greet("Markdown")
```

### Обычный блок кода (без указания языка)
---

## Цитаты

### Обычная цитата
> Markdown позволяет программистам быстро и аккуратно оформлять файлы документации.

### Вложенная цитата
> Первый уровень цитирования
>> Второй уровень цитирования (вложенный текст)